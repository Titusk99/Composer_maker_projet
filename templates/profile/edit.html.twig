{% extends 'base.html.twig' %}

{% block title %}Modifier mon profil{% endblock %}

{% block body %}
<h1>Modifier mon profil</h1>

{{ form_start(profileForm) }}
    <div>
        {{ form_label(profileForm.nom) }}
        {{ form_widget(profileForm.nom) }}
        {{ form_errors(profileForm.nom) }}
    </div>

    <div>
        {{ form_label(profileForm.email) }}
        {{ form_widget(profileForm.email) }}
        {{ form_errors(profileForm.email) }}
    </div>

    <button type="submit">Enregistrer les modifications</button>
    <a href="{{ path('app_profile') }}">Annuler</a>
{{ form_end(profileForm) }}

<hr>

<p>
    <strong>Mot de passe :</strong> 
    Pour changer votre mot de passe, <a href="{{ path('app_forgot_password_request') }}">cliquez ici</a>. 
    Un email vous sera envoyé pour confirmer la modification.
</p>
{% endblock %}
